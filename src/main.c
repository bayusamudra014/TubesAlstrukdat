#include <stdio.h>

#include "views/mainmenu/mainmenu.h"
#include "views/views.h"

int main() {
  // char file_name[50];
  // scanf(" %[^\n]s", file_name);

  show_main_menu();
  // lx_saveToFile("save.conf");
  return 0;
}
